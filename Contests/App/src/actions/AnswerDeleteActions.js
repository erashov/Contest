import * as types from './ActionTypes.js';

export const openForm = item => ({ type: types.ANSWER_FORM_DELETE_OPEN, item });
export const closeForm = () => ({ type: types.ANSWER_FORM_DELETE_CLOSE });
